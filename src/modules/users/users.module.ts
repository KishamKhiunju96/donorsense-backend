import { Module } from '@nestjs/common';

/**
 * UsersModule — stub placeholder.
 * Full implementation in SKILL-module.md.
 * Will provide UsersController, UsersService, User entity, DTOs, etc.
 */
@Module({})
export class UsersModule {}
